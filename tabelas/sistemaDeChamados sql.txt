create database sistemadechamados;

use sistemadechamados;

CREATE TABLE usuarios (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nome VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL UNIQUE,
    senha VARCHAR(255) NOT NULL, -- Guardar sempre como hash (ex: bcrypt)
    perfil ENUM('admin', 'normal') NOT NULL DEFAULT 'normal',
    id_cliente INT(11) NOT NULL, -- Mudou para bater com o tipo da tabela clientes
    criado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    atualizado_em TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    CONSTRAINT fk_usuario_cliente 
        FOREIGN KEY (id_cliente) 
        REFERENCES clientes (id_cliente) 
        ON DELETE RESTRICT 
        ON UPDATE CASCADE
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;

 CREATE TABLE `clientes` (
  `id_cliente` int(11) NOT NULL AUTO_INCREMENT,
  `nome_empresa` varchar(255) NOT NULL,
  `localizacao` text DEFAULT NULL,
  `contato_principal` varchar(100) DEFAULT NULL,
  `num_celular` varchar(20) DEFAULT NULL,
  `email_contato` varchar(100) NOT NULL,
  `status_cliente` enum('Ativo','Inativo') DEFAULT 'Ativo',
  PRIMARY KEY (`id_cliente`)
) ENGINE=InnoDB AUTO_INCREMENT=7 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci

CREATE TABLE `tecnicos` (
  `id_tecnico` int(11) NOT NULL AUTO_INCREMENT,
  `nome_tecnico` varchar(100) NOT NULL,
  `email` varchar(100) DEFAULT NULL,
  `num_celular` varchar(20) DEFAULT NULL,
  `status_tecnico` enum('Ativo','Inativo') DEFAULT 'Ativo',
  `localizacao` varchar(255) DEFAULT NULL,
  PRIMARY KEY (`id_tecnico`),
  UNIQUE KEY `email` (`email`)
) ENGINE=InnoDB AUTO_INCREMENT=3 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci

CREATE TABLE `chamados` (
  `id_chamado` int(11) NOT NULL AUTO_INCREMENT,
  `id_cliente` int(11) NOT NULL,
  `id_tecnico_atribuido` int(11) DEFAULT NULL,
  `data_abertura` datetime NOT NULL DEFAULT current_timestamp(),
  `data_fechamento` datetime DEFAULT NULL,
  `status` enum('Novo','Em Atendimento','Aguardando Cliente','Conclu?do') NOT NULL DEFAULT 'Novo',
  `prioridade` enum('Baixa','M?dia','Alta','Urgente') NOT NULL DEFAULT 'M?dia',
  `descricao_solicitacao` text NOT NULL,
  `origem` varchar(50) DEFAULT NULL,
  `solucao` text DEFAULT NULL,
  PRIMARY KEY (`id_chamado`),
  KEY `id_cliente` (`id_cliente`),
  KEY `id_tecnico_atribuido` (`id_tecnico_atribuido`),
  CONSTRAINT `chamados_ibfk_1` FOREIGN KEY (`id_cliente`) REFERENCES `clientes` (`id_cliente`),
  CONSTRAINT `chamados_ibfk_2` FOREIGN KEY (`id_tecnico_atribuido`) REFERENCES `tecnicos` (`id_tecnico`)
) ENGINE=InnoDB AUTO_INCREMENT=3 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci

------------------------------------------------------------
------------------------------------------------------------
------------------------------------------------------------
--Para funcionar a primeira vez (mude os valores para o contato admin)
INSERT INTO clientes (
    id_cliente, 
    nome_empresa, 
    localizacao, 
    contato_principal, 
    num_celular, 
    email_contato, 
    status_cliente
) VALUES (
    1, 
    'Empresa Teste', 
    'Localizacao Teste', 
    'Contato Teste', 
    '00000000000', 
    'teste@teste.com.br', 
    'Ativo'
);

